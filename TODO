* Crear el concepto de generator que sea un directorio con la siguiente estructura:

generator/model.yaml
generator/templates

* Al template agregarle el mode del archivo a generar

* Cambiar el name de skeleton por algo mas como destination asi queda claro que ahi se tiran los archivos.

* En el template chequear que el sourcde exista.
